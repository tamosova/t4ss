/* import { ParentSelfValidatorDirective } from './parents-self.directive';

describe('ParentsSelfDirective', () => {
  it('should create an instance', () => {
    const directive = new ParentSelfValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
 */